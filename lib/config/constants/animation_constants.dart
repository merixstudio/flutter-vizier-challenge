part of 'app_constants.dart';

class _AnimationConstants {
  const _AnimationConstants();

  Duration get defaultDuration => const Duration(
        milliseconds: 300,
      );
  Duration get pieChartSweep => const Duration(
        seconds: 1,
      );
}
